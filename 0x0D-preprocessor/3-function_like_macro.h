#ifndef MACROABC_H
#define MACROABC_H

/**
 *File: 3-function_like_ macro
 *
 *@x: the integer value to compute the value
 *Desc: Header file to compute absolute value of given number
 */

#define ABS(x) ((x) < 0 ? -(1) * (x) : (x))

#endif
